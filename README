These are the instructions to run the notify program.
There are complining instructions and inputs that can be provided to run this process,
these instructions are due to the Project instruction PDF that was given in the moodle system.


Compiling instruction:

	gcc -g -finstrument-functions notify.c -o notify -lcli -lpthread


insputs options:

1. notify flag inputs exmaples
	
	sudo ./notify -i 127.0.0.1 -d ./ 

2. telnet inputs 
	
	telnet localhost 12345

	UserName:  UnixClass
	PassWord: 1

	backtrace

	exit (to finish)

3. netcat inputs
	
	netcat -lukp 5000	
	

